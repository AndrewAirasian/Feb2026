# Feb2026

# Andrew's README document

Using git.

This will be on the third merge. I have become confident with using git add, commit, branch, checkout, and merge commands. When I do the ssh -T git@github.com, it says that I've successfully authenticated but that github does not provide shell access.